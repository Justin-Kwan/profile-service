type CourierInput {
  firstName: String
  lastName: String
  email: String
  country: String
  locationId: String
  mobileNum: String
  orderZoneId: String
}

mutation createPost(input: CourierInput!): CreatePostPayload!

type CreatePostPayload {
  post: Post
}
